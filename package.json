{
  "name": "django",
  "version": "1.0.0",
  "description": "Refresh wsgi.py so that Django rebuilds on run",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "City of Tucson",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-notify": "^3.2.0",
    "gulp-touch-cmd": "0.0.1"
  }
}
